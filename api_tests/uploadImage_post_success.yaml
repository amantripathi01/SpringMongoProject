# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=354b987cbe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "uploadImage_post_success.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true

scenarios:
  - flow:
      - post:
          url: "/pet/{{ petId }}/uploadImage"
          beforeRequest: "setHeaders"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          capture:
            - json: "$"
              as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "code"
            - hasProperty: "type"
            - hasProperty: "message"
          log: "Post request to /pet/{{ petId }}/uploadImage was successful"
      - think: 1

functions:
  setHeaders:
    - function: "setHeaders"
      options:
        Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        Content-Type: "multipart/form-data"

