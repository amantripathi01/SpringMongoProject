# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "pet_post_success.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Add a new pet to the store"
    flow:
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
          log: "Pet object that needs to be added to the store"
        think: 1
