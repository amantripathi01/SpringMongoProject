# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=354b987cbe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "uploadImage_post_others.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Upload Image API failure cases"
    flow:
      - log: "Invalid Pet ID"
        post:
          url: "/pet/invalidID/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            petId: "{{ petId }}"
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404 # Pet not found
            - hasProperty: "message"
        think: 1

      - log: "Missing required field 'Pet ID'"
        post:
          url: "/pet//uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 400 # Bad request
            - hasProperty: "message"
        think: 1

      - log: "Invalid URL"
        post:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            petId: "{{ petId }}"
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404 # Not Found
            - hasProperty: "message"
        think: 1

      - log: "Invalid Authorization Token"
        post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "InvalidToken"
          formData:
            petId: "{{ petId }}"
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"
        think: 1
