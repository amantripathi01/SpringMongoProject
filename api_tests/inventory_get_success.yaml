# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processors: {}
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "inventory_get_success.csv"
    fields:
      - "status"
    skipHeader: true
  environments:
    dev:
      target: "http://dev.example.com"
      phases:
        - duration: 60
          arrivalRate: 20
scenarios:
  - name: "Get inventory scenario"
    flow:
      - get:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$"
            as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - hasHeader: "content-type"
          log: "Get inventory request is successful"
      - think: 1
