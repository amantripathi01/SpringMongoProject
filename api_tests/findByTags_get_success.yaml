# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "findByTags_get_success.csv"
    fields: ["tags"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Find pets by tags"
    flow:
      - log: "Find pets by tags API test started"
      - get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
            - matchJSON: 
                selector: ".status"
                value: "available"
      - think: 1
