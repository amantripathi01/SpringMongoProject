# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_others.csv"
    fields: ["status"]
    skipHeader: true
  environments:
    test:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Petstore API test scenario"
    flow:
      - log: "Making a request to an invalid URL"
        get:
          url: "/invalidURL"
          headers:
            Authorization: "InvalidAuth"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Making a request with invalid status value"
        get:
          url: "/pet/findByStatus?status=invalidStatus"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals:
                description: "Invalid status value"
      - think: 1
      - log: "Making a request without authorization"
        get:
          url: "/pet/findByStatus?status={{ status }}"
          expect:
            - statusCode: 401
      - think: 1
