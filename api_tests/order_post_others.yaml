# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=036bac41de

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order_post_others.csv"
    fields:
      - "id"
      - "petId"
      - "quantity"
      - "shipDate"
      - "status"
      - "complete"
    skipHeader: true

scenarios:
  - name: "Place an order for a pet - Failure cases"
    flow:
      - think: 5
      - post:
          url: "/store/order"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          log: "Posting invalid order payload"
          expect:
            - statusCode: 400
            - hasProperty: "description"
      - think: 5
      - post:
          url: "/invalid/url"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          log: "Posting to invalid URL"
          expect:
            - statusCode: 404
      - think: 5
      - post:
          url: "/store/order"
          headers:
            authorization: "InvalidToken"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          log: "Posting with invalid authorization"
          expect:
            - statusCode: 401
