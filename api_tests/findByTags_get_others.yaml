# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "findByTags_get_others.csv"
    fields: ["tags"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Testing failure and edge cases for findByTags API"
    flow:
      - log: "Testing the scenario where no tags are provided"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
      - think: 1
      - log: "Testing the scenario where invalid URL is used"
        get:
          url: "/pet/findByTagsInvalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing the scenario where invalid authorization token is used"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing the scenario where query parameter is not as per the API spec"
        get:
          url: "/pet/findByTags?invalidParam=invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
