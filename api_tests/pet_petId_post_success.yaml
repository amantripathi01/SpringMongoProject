# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=99d9f02040

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_post_success.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true

scenarios:
  - name: "Update pet with form data"
    flow:
      - post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
            - bodyContains:
                - "{{ name }}"
                - "{{ status }}"
          beforeRequest:
            think: 1
            log: "Sending request to update pet with petId: {{ petId }}, name: {{ name }}, status: {{ status }}"
