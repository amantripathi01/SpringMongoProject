# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "inventory_get_others.csv"
    fields: ["status"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "API Robustness Testing"
    flow:
      - log: "Invalid URL Request"
        get:
          url: "/store/invalidurl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
      - log: "Request without Authorization"
        get:
          url: "/store/inventory"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
      - log: "Invalid Authorization Request"
        get:
          url: "/store/inventory"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
            - hasProperty: "error"
      - think: 1
      - log: "Request with Invalid Payload"
        post:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            status: "{{ status }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
