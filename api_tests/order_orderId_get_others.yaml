# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=74f49e1ef7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true
scenarios:
  - name: "Failure and edge cases for /store/order/{orderId} endpoint"
    flow:
      - think: 5
      - log: "Sending request with invalid orderId"
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - body: "Invalid ID supplied"
      - think: 5
      - log: "Sending request with non existent orderId"
        get:
          url: "/store/order/9999999999"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - body: "Order not found"
      - think: 5
      - log: "Sending request with invalid URL"
        get:
          url: "/store/invalidURL/{{ orderId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5
      - log: "Sending request with invalid Authorization token"
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "Bearer invalidToken"
          expect:
            - statusCode: 401
