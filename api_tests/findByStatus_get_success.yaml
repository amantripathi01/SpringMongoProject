# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "findByStatus_get_success.csv"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Find Pets by status"
    flow:
      - get:
          url: "/pet/findByStatus"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          qs:
            status: "{{ status }}"
          capture:
            - json: "$"
              as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "responseBody"
            - hasProperty: "responseBody[0].name"
            - hasProperty: "responseBody[0].photoUrls"
          log: "Find Pets by status successful."
      - think: 1
