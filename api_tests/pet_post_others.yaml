# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_post_others.csv"
    fields:
      - "id"
      - "category"
      - "name"
      - "photoUrls"
      - "tags"
      - "status"
    skipHeader: true
scenarios:
  - name: "Invalid API calls to /pet"
    flow:
      - think: 1
      - log: "Sending a POST request with empty payload"
        post:
          url: "/pet"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json: {}
          expect:
            - statusCode: 405
            - hasProperty: "description"
            - equals:
                description: "Invalid input"
      - think: 1
      - log: "Sending a POST request with missing required fields"
        post:
          url: "/pet"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
            - equals:
                description: "Invalid input"
      - think: 1
      - log: "Sending a POST request with invalid authorization token"
        post:
          url: "/pet"
          headers:
            Authorization: "Bearer invalid_token"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Sending a POST request to an invalid URL"
        post:
          url: "/invalid_url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
