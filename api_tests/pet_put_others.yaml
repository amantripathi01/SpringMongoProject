# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a21296b4f6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_put_others.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
    skipHeader: true
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Pet API Failure and Edge Test Cases"
    flow:
      - post:
          url: "/pet"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "invalid_status"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
          beforeRequest:
            log: "Sending invalid status to check the validation exception"
      - think: 1
      - post:
          url: "/invalid_pet_url"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          beforeRequest:
            log: "Sending request to invalid URL to check the 404 response"
      - think: 1
      - post:
          url: "/pet"
          json:
            id: "invalid_id"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          beforeRequest:
            log: "Sending invalid ID to check the 400 response"
      - think: 1
      - post:
          url: "/pet"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
          beforeRequest:
            log: "Sending invalid authorization token to check the 401 response"
