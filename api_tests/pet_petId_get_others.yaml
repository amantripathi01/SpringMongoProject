# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2c3a7f4837

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_others.csv"
    fields:
      - "petId"
    skipHeader: true
scenarios:
  - name: "Testing edge, failure and error handling cases for /pet/{petId} endpoint"
    flow:
      - log: "Sending request with invalid petId"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Sending request with petId not found in database"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1
      - log: "Sending request with invalid URL"
        get:
          url: "/invalidURL/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Sending request with invalid Authorization token"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
        think: 1
