# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_success.csv"
    fields:
      - "petId"
    skipHeader: true

scenarios:
  - name: "Delete pet scenario"
    flow:
      - log: "Delete pet request"
        delete:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "test_api_key"
          expect:
            - statusCode: 200
            - hasProperty: "message"
        think: 1
