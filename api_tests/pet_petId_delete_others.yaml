# Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 120
      arrivalRate: 20
  payload:
    path: "pet_petId_delete_others.csv"
    fields: ["petId", "api_key"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Delete Pet API test"
    flow:
      - log: "Sending a DELETE request to /pet/{petId} with invalid petId"
        delete:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
              - "{{ response.body.description }}": "Invalid pet value"
      - think: 5
      - log: "Sending a DELETE request to /pet/{petId} with invalid api_key"
        delete:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "InvalidApiKey"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
              - "{{ response.body.description }}": "Invalid pet value"
      - think: 5
      - log: "Sending a DELETE request to /pet/{petId} with invalid Authorization"
        delete:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "InvalidAuthToken"
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - equals: 
              - "{{ response.body.description }}": "Invalid token supplied"
      - think: 5
      - log: "Sending a DELETE request to /pet/{petId} with invalid URL"
        delete:
          url: "/invalid/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
              - "{{ response.body.description }}": "Invalid URL"
