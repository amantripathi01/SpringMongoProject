// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Entity;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.MockitoAnnotations;
import org.springframework.boot.test.context.SpringBootTest;
import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
public class Student_158c71b90f_Test {

    private Student student;

    @BeforeEach
    public void setUp() {
        MockitoAnnotations.openMocks(this);
        student = new Student();
    }

    @Test
    public void testStudent() {
        String _id = "123";
        String studentname = "John";
        String studentaddress = "123 Main St";
        String mobile = "1234567890";

        student = new Student(_id, studentname, studentaddress, mobile);

        assertEquals(_id, student._id);
        assertEquals(studentname, student.studentname);
        assertEquals(studentaddress, student.studentaddress);
        assertEquals(mobile, student.mobile);
    }

    @Test
    public void testStudentFailure() {
        String _id = "123";
        String studentname = "John";
        String studentaddress = "123 Main St";
        String mobile = "1234567890";

        student = new Student(_id, studentname, studentaddress, mobile);

        assertNotEquals("456", student._id);
        assertNotEquals("Doe", student.studentname);
        assertNotEquals("456 Main St", student.studentaddress);
        assertNotEquals("0987654321", student.mobile);
    }
}
