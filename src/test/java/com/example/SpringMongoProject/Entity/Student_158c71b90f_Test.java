// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Entity;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class Student_158c71b90f_Test {

    @Test
    public void testStudentConstructor() {
        String _id = "1";
        String studentname = "John Doe";
        String studentaddress = "123 Street";
        String mobile = "1234567890";
        
        Student student = new Student(_id, studentname, studentaddress, mobile);

        assertEquals(_id, student.get_id(), "The ID should match the constructor argument");
        assertEquals(studentname, student.getStudentname(), "The student name should match the constructor argument");
        assertEquals(studentaddress, student.getStudentaddress(), "The student address should match the constructor argument");
        assertEquals(mobile, student.getMobile(), "The mobile should match the constructor argument");
    }

    @Test
    public void testStudentConstructorWithEmptyStrings() {
        String _id = "";
        String studentname = "";
        String studentaddress = "";
        String mobile = "";
        
        Student student = new Student(_id, studentname, studentaddress, mobile);

        assertEquals(_id, student.get_id(), "The ID should match the constructor argument even if it's an empty string");
        assertEquals(studentname, student.getStudentname(), "The student name should match the constructor argument even if it's an empty string");
        assertEquals(studentaddress, student.getStudentaddress(), "The student address should match the constructor argument even if it's an empty string");
        assertEquals(mobile, student.getMobile(), "The mobile should match the constructor argument even if it's an empty string");
    }
}
