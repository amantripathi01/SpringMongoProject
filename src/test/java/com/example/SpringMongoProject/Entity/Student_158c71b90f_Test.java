// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Entity;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Assertions;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.beans.factory.annotation.Autowired;

@SpringBootTest
public class Student_158c71b90f_Test {

    @Autowired
    private Student student;

    @MockBean
    private StudentRepository studentRepository;

    @BeforeEach
    public void setUp() {
        student = new Student("1", "John", "New York", "1234567890");
    }

    @Test
    public void testStudentCreation() {
        Assertions.assertEquals("1", student.get_id());
        Assertions.assertEquals("John", student.getStudentName());
        Assertions.assertEquals("New York", student.getStudentAddress());
        Assertions.assertEquals("1234567890", student.getMobile());
    }

    @Test
    public void testStudentCreationFailure() {
        Assertions.assertThrows(IllegalArgumentException.class, () -> {
            new Student(null, "John", "New York", "1234567890");
        });

        Assertions.assertThrows(IllegalArgumentException.class, () -> {
            new Student("1", null, "New York", "1234567890");
        });

        Assertions.assertThrows(IllegalArgumentException.class, () -> {
            new Student("1", "John", null, "1234567890");
        });

        Assertions.assertThrows(IllegalArgumentException.class, () -> {
            new Student("1", "John", "New York", null);
        });
    }

}
