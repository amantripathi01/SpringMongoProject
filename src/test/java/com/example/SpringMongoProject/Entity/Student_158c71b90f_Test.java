// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Entity;

import org.junit.Before;
import org.junit.Test;
import org.springframework.data.annotation.Id;
import org.springframework.data.mongodb.core.mapping.Document;
import static org.junit.Assert.assertEquals;

public class Student_158c71b90f_Test {
    private Student student;

    @Before
    public void setUp() {
        student = new Student("1", "John Doe", "123 Main St", "1234567890");
    }

    @Test
    public void testConstructor() {
        assertEquals("1", student.get_id());
        assertEquals("John Doe", student.getStudentname());
        assertEquals("123 Main St", student.getStudentaddress());
        assertEquals("1234567890", student.getMobile());
    }

    @Test
    public void testSettersAndGetters() {
        student.set_id("2");
        student.setStudentname("Jane Doe");
        student.setStudentaddress("456 Main St");
        student.setMobile("0987654321");

        assertEquals("2", student.get_id());
        assertEquals("Jane Doe", student.getStudentname());
        assertEquals("456 Main St", student.getStudentaddress());
        assertEquals("0987654321", student.getMobile());
    }
}
