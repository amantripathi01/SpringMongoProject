// Test generated by RoostGPT for test gitlab using AI Type Open AI and AI Model gpt-4-1106-preview

package com.example.SpringMongoProject.Entity;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class Student_158c71b90f_Test {

    @Test
    public void testStudentConstructorWithAllFields() {
        // Create a Student instance using the constructor with parameters
        Student student = new Student("123", "John Doe", "123 Main St", "555-1234");

        // Assertions to check if the Student object fields are correctly set
        assertEquals("123", student.get_id(), "The student _id should be 123");
        assertEquals("John Doe", student.getStudentname(), "The student name should be John Doe");
        assertEquals("123 Main St", student.getStudentaddress(), "The student address should be 123 Main St");
        assertEquals("555-1234", student.getMobile(), "The student mobile should be 555-1234");
    }

    @Test
    public void testStudentConstructorWithNoFields() {
        // Create a Student instance using the constructor without parameters
        Student student = new Student();

        // Assertions to check if the Student object fields are null or empty
        assertEquals(null, student.get_id(), "The student _id should be null");
        assertEquals(null, student.getStudentname(), "The student name should be null");
        assertEquals(null, student.getStudentaddress(), "The student address should be null");
        assertEquals(null, student.getMobile(), "The student mobile should be null");
    }
}
