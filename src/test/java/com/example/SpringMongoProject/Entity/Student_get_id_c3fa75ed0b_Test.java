// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Entity;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

@SpringBootTest
public class Student_get_id_c3fa75ed0b_Test {

    @Autowired
    private Student student;

    @MockBean
    private StudentRepository studentRepository;

    @Test
    public void testGetIdSuccess() {
        String expected = "123456";
        student.set_id(expected);
        String actual = student.get_id();
        Assertions.assertEquals(expected, actual, "The expected ID does not match the actual ID.");
    }

    @Test
    public void testGetIdFailure() {
        String unexpected = "654321";
        student.set_id("123456");
        String actual = student.get_id();
        Assertions.assertNotEquals(unexpected, actual, "The unexpected ID should not match the actual ID.");
    }
}
