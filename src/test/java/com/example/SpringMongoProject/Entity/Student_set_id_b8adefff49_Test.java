// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Entity;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import static org.junit.jupiter.api.Assertions.assertEquals;

@SpringBootTest
public class Student_set_id_b8adefff49_Test {

    @Test
    public void testSet_id_ValidId() {
        Student student = new Student();
        String id = "12345";
        student.set_id(id);
        assertEquals(id, student.get_id(), "The id must be the same as the one set");
    }

    @Test
    public void testSet_id_EmptyId() {
        Student student = new Student();
        String id = "";
        student.set_id(id);
        assertEquals(id, student.get_id(), "The id must be the same as the one set, even if it's empty");
    }
}
