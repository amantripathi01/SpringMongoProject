// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Entity;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.data.annotation.Id;
import org.springframework.data.mongodb.core.mapping.Document;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.*;

@SpringBootTest
public class Student_getStudentname_b7193f1cfc_Test {

    @MockBean
    private Student student;

    @Test
    public void testGetStudentName() {
        when(student.getStudentname()).thenReturn("John Doe");
        String studentName = student.getStudentname();
        assertEquals("John Doe", studentName);
    }

    @Test
    public void testGetStudentName_Empty() {
        when(student.getStudentname()).thenReturn("");
        String studentName = student.getStudentname();
        assertEquals("", studentName);
    }
}
