/*
Test generated by RoostGPT for test aman321 using AI Type Open AI and AI Model gpt-4

1. **Scenario:** Verify if the function accepts string values and sets the student name correctly.
   **Steps:** Pass a valid string value to the function and check if the student name is set correctly.

2. **Scenario:** Verify if the function correctly handles null values.
   **Steps:** Pass a null value to the function and check how it handles the scenario.

3. **Scenario:** Verify if the function correctly handles empty string values.
   **Steps:** Pass an empty string value to the function and check if the student name is set to an empty string.

4. **Scenario:** Verify if the function correctly handles string values with leading and trailing whitespaces.
   **Steps:** Pass a string value with leading and trailing whitespaces to the function and check if the student name is set correctly.

5. **Scenario:** Verify if the function correctly handles string values with special characters.
   **Steps:** Pass a string value with special characters to the function and check if the student name is set correctly.

6. **Scenario:** Verify if the function correctly handles string values with numbers.
   **Steps:** Pass a string value with numbers to the function and check if the student name is set correctly.

7. **Scenario:** Verify if the function correctly handles string values with a mix of uppercase and lowercase letters.
   **Steps:** Pass a string value with a mix of uppercase and lowercase letters to the function and check if the student name is set correctly.

8. **Scenario:** Verify if the function correctly handles string values with non-English characters.
   **Steps:** Pass a string value with non-English characters to the function and check if the student name is set correctly.

9. **Scenario:** Verify if the function correctly handles long string values.
   **Steps:** Pass a long string value to the function and check how it handles the scenario.

10. **Scenario:** Verify if the function correctly handles the scenario when called multiple times with the same student name.
    **Steps:** Call the function multiple times with the same student name and check if it sets the student name correctly each time. 

11. **Scenario:** Verify if the function correctly handles the scenario when called multiple times with different student names.
    **Steps:** Call the function multiple times with different student names and check if it sets the student name correctly each time.
*/
import org.springframework.data.annotation.Id;
import org.springframework.data.mongodb.core.mapping.Document;

@Document(collection = "Student")
public class Student {
    @Id
    private String _id;
    private String studentname;
    private String studentaddress;
    private String mobile;

    public String get_id() {
        return _id;
    }

    public void set_id(String _id) {
        this._id = _id;
    }

    public String getStudentname() {
        return studentname;
    }

    public void setStudentname(String studentname) {
        this.studentname = studentname;
    }

    public String getStudentaddress() {
        return studentaddress;
    }

    public void setStudentaddress(String studentaddress) {
        this.studentaddress = studentaddress;
    }

    public String getMobile() {
        return mobile;
    }

    public void setMobile(String mobile) {
        this.mobile = mobile;
    }

    @Override
    public String toString() {
        return "Student{" + "_id='" + _id + '\'' + ", studentname='" + studentname + '\'' + ", studentaddress='" + studentaddress + '\'' + ", mobile='" + mobile + '\'' + '}';
    }
}
