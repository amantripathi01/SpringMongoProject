// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

@SpringBootTest
public class StudentServices_deleteStudent_9983cca4b5_Test {

    @InjectMocks
    private StudentServices studentServices;

    @MockBean
    private StudentRepo repo;

    @BeforeEach
    public void setUp() {
        MockitoAnnotations.openMocks(this);
    }

    @Test
    public void testDeleteStudent_success() {
        String studentId = "12345";
        Mockito.doNothing().when(repo).deleteById(studentId);
        studentServices.deleteStudent(studentId);
        Mockito.verify(repo, Mockito.times(1)).deleteById(studentId);
    }

    @Test
    public void testDeleteStudent_failure() {
        String studentId = "12345";
        Mockito.doThrow(new RuntimeException()).when(repo).deleteById(studentId);
        Assertions.assertThrows(RuntimeException.class, () -> {
            studentServices.deleteStudent(studentId);
        });
        Mockito.verify(repo, Mockito.times(1)).deleteById(studentId);
    }
}
