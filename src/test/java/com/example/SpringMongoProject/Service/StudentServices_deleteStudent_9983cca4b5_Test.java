// Test generated by RoostGPT for test gitlab using AI Type Open AI and AI Model gpt-4-1106-preview

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
public class StudentServices_deleteStudent_9983cca4b5_Test {

    @Mock
    private StudentRepo repo;

    @InjectMocks
    private StudentServices studentServices;

    @BeforeEach
    public void setUp() {
        // TODO: This is a placeholder for any setup if necessary before each test
    }

    @Test
    public void testDeleteStudent_success() {
        // Arrange
        String studentId = "123"; // TODO: Replace with actual student ID
        doNothing().when(repo).deleteById(studentId);

        // Act
        studentServices.deleteStudent(studentId);

        // Assert
        verify(repo, times(1)).deleteById(studentId);
    }

    @Test
    public void testDeleteStudent_failure() {
        // Arrange
        String studentId = "non-existent"; // TODO: Replace with actual non-existent student ID
        doThrow(new IllegalArgumentException("Student not found")).when(repo).deleteById(studentId);

        try {
            // Act
            studentServices.deleteStudent(studentId);
        } catch (IllegalArgumentException e) {
            // Assert
            verify(repo, times(1)).deleteById(studentId);
            // Additional assertions can be done on exception message if needed
        }
    }
}
