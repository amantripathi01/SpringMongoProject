// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

@SpringBootTest
public class StudentServices_deleteStudent_9983cca4b5_Test {

    @Autowired
    private StudentServices studentServices;

    @MockBean
    private StudentRepo repo;

    @BeforeEach
    public void setup() {
        Student student = new Student();
        student.set_id("1");
        student.setStudentname("John");
        student.setStudentaddress("123 Street");
        student.setMobile("1234567890");

        Mockito.when(repo.findById("1")).thenReturn(Optional.of(student));
        Mockito.doNothing().when(repo).deleteById("1");
    }

    @Test
    public void testDeleteStudent_success() {
        Assertions.assertDoesNotThrow(() -> studentServices.deleteStudent("1"));
        Mockito.verify(repo, Mockito.times(1)).deleteById("1");
    }

    @Test
    public void testDeleteStudent_failure() {
        Assertions.assertThrows(IllegalArgumentException.class, () -> studentServices.deleteStudent("2"));
        Mockito.verify(repo, Mockito.times(0)).deleteById("2");
    }
}
