// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import static org.mockito.Mockito.doNothing;
import static org.mockito.Mockito.times;
import static org.mockito.Mockito.verify;

@ExtendWith(MockitoExtension.class)
public class StudentServices_deleteStudent_9983cca4b5_Test {

    @InjectMocks
    private StudentServices studentServices;

    @Mock
    private StudentRepo studentRepo;

    @Test
    public void testDeleteStudent_success() {
        String id = "testId";
        doNothing().when(studentRepo).deleteById(id);

        studentServices.deleteStudent(id);

        verify(studentRepo, times(1)).deleteById(id);
    }

    @Test
    public void testDeleteStudent_failure() {
        String id = "testId";
        doNothing().when(studentRepo).deleteById(id);

        studentServices.deleteStudent("wrongId");

        verify(studentRepo, times(0)).deleteById(id);
    }
}
