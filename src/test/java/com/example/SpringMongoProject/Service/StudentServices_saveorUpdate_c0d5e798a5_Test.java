// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.junit.MockitoJUnitRunner;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.mockito.Mockito.*;

@RunWith(MockitoJUnitRunner.class)
@SpringBootTest
public class StudentServices_saveorUpdate_c0d5e798a5_Test {

    @InjectMocks
    StudentService studentService;

    @Mock
    StudentRepo repo;

    Student students;

    @Before
    public void setUp() {
        students = new Student();
        students.set_id("123");
        students.setStudentname("John");
        students.setStudentaddress("123 St");
        students.setMobile("1234567890");
    }

    @Test
    public void testSaveorUpdate() {
        doNothing().when(repo).save(students);
        studentService.saveorUpdate(students);
        verify(repo, times(1)).save(students);
    }

    @Test(expected = Exception.class)
    public void testSaveorUpdate_Exception() {
        doThrow(new Exception()).when(repo).save(students);
        studentService.saveorUpdate(students);
    }
}
