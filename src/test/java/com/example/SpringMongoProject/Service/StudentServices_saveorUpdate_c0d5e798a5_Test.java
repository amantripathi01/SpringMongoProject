// Test generated by RoostGPT for test gitlab using AI Type Open AI and AI Model gpt-4-1106-preview

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import static org.mockito.Mockito.*;

import static org.junit.jupiter.api.Assertions.*;

@ExtendWith(MockitoExtension.class)
public class StudentServices_saveorUpdate_c0d5e798a5_Test {

    @Mock
    private StudentRepo repo;

    @InjectMocks
    private StudentServices studentServices;

    private Student student1;
    private Student student2;

    @BeforeEach
    void setUp() {
        student1 = new Student(); // TODO: Populate student1 fields
        student2 = new Student(); // TODO: Populate student2 fields with different values
    }

    @Test
    public void testSaveorUpdate_Success() {
        doNothing().when(repo).save(any(Student.class));

        assertDoesNotThrow(() -> studentServices.saveorUpdate(student1));

        verify(repo, times(1)).save(student1);
    }

    @Test
    public void testSaveorUpdate_Failure() {
        doThrow(new RuntimeException("Save failed")).when(repo).save(any(Student.class));

        Exception exception = assertThrows(RuntimeException.class, () -> studentServices.saveorUpdate(student2));

        assertEquals("Save failed", exception.getMessage());
        verify(repo, times(1)).save(student2);
    }
}
