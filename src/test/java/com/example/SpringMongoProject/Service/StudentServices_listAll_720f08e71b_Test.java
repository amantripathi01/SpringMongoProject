// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.boot.test.context.SpringBootTest;

import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;

@SpringBootTest
@ExtendWith(MockitoExtension.class)
public class StudentServices_listAll_720f08e71b_Test {

    @Mock
    private StudentRepo repo;

    @InjectMocks
    private StudentServices studentServices;

    @Test
    public void testListAll() {
        Student studentOne = new Student();
        studentOne.setName("John");
        Student studentTwo = new Student();
        studentTwo.setName("Jane");

        List<Student> students = Arrays.asList(studentOne, studentTwo);
        when(repo.findAll()).thenReturn(students);

        Iterable<Student> returnedStudents = studentServices.listAll();

        assertEquals(students, returnedStudents);
    }

    @Test
    public void testListAll_whenNoStudents() {
        List<Student> students = Arrays.asList();
        when(repo.findAll()).thenReturn(students);

        Iterable<Student> returnedStudents = studentServices.listAll();

        assertEquals(students, returnedStudents);
    }
}
