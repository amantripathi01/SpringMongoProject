// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import static org.mockito.Mockito.*;
import static org.assertj.core.api.Assertions.assertThat;

import java.util.Arrays;
import java.util.List;

@ExtendWith(MockitoExtension.class)
public class StudentServices_listAll_720f08e71b_Test {

    @InjectMocks
    private StudentServices studentServices;

    @Mock
    private StudentRepo studentRepo;

    @Test
    public void testListAll() {
        Student student1 = new Student();
        student1.set_studentId("123");
        student1.set_studentName("John");

        Student student2 = new Student();
        student2.set_studentId("456");
        student2.set_studentName("Jane");

        List<Student> studentList = Arrays.asList(student1, student2);

        when(studentRepo.findAll()).thenReturn(studentList);

        Iterable<Student> result = studentServices.listAll();

        assertThat(result).hasSize(2).contains(student1, student2);
    }

    @Test
    public void testListAll_Empty() {
        when(studentRepo.findAll()).thenReturn(Arrays.asList());

        Iterable<Student> result = studentServices.listAll();

        assertThat(result).isEmpty();
    }
}
