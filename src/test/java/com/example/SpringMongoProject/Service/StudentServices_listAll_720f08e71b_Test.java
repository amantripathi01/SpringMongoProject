// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
public class StudentServices_listAll_720f08e71b_Test {

    @InjectMocks
    private StudentServices studentServices;

    @Mock
    private StudentRepo studentRepo;

    @Test
    public void testListAll_studentsExist() {
        Student student1 = new Student();
        student1.set_id("1");
        student1.setStudentname("John");
        student1.setStudentaddress("NY");
        student1.setMobile("1234567890");

        Student student2 = new Student();
        student2.set_id("2");
        student2.setStudentname("Doe");
        student2.setStudentaddress("LA");
        student2.setMobile("0987654321");

        List<Student> students = Arrays.asList(student1, student2);

        when(studentRepo.findAll()).thenReturn(students);

        Iterable<Student> result = studentServices.listAll();

        // TODO: Change the expected value based on your actual data
        assertEquals(students, result);
    }

    @Test
    public void testListAll_noStudentsExist() {
        List<Student> students = Arrays.asList();

        when(studentRepo.findAll()).thenReturn(students);

        Iterable<Student> result = studentServices.listAll();

        // TODO: Change the expected value based on your actual data
        assertEquals(students, result);
    }
}
