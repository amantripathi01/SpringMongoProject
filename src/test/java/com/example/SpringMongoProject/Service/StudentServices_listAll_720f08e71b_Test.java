// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Service;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Repo.StudentRepo;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.beans.factory.annotation.Autowired;

import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
public class StudentServices_listAll_720f08e71b_Test {

    @Mock
    private StudentRepo repo;

    @Autowired
    @InjectMocks
    private StudentService studentService;

    @Test
    public void testListAll_WhenStudentsExist() {
        Student student1 = new Student("1", "John", "New York", "1234567890");
        Student student2 = new Student("2", "Emma", "Chicago", "0987654321");
        List<Student> expectedStudents = Arrays.asList(student1, student2);

        when(repo.findAll()).thenReturn(expectedStudents);

        Iterable<Student> actualStudents = studentService.listAll();

        assertEquals(expectedStudents, actualStudents);
    }

    @Test
    public void testListAll_WhenNoStudentsExist() {
        List<Student> expectedStudents = Arrays.asList();

        when(repo.findAll()).thenReturn(expectedStudents);

        Iterable<Student> actualStudents = studentService.listAll();

        assertEquals(expectedStudents, actualStudents);
    }
}
