// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class StudentController_update_7f4e3cda7f_Test {

    @InjectMocks
    StudentController studentController;

    @Mock
    StudentServices studentServices;

    @Test
    public void testUpdateStudent_Success() {
        Student student = new Student();
        student.set_id("123");
        student.setStudentname("John Doe");
        student.setStudentaddress("123 St");
        student.setMobile("1234567890");

        Mockito.doNothing().when(studentServices).saveorUpdate(Mockito.any(Student.class));

        Student result = studentController.update(student, "123");

        Assertions.assertEquals("123", result.get_id());
        Assertions.assertEquals("John Doe", result.getStudentname());
        Assertions.assertEquals("123 St", result.getStudentaddress());
        Assertions.assertEquals("1234567890", result.getMobile());
    }

    @Test
    public void testUpdateStudent_Failure() {
        Student student = new Student();
        student.set_id("123");
        student.setStudentname("John Doe");
        student.setStudentaddress("123 St");
        student.setMobile("1234567890");

        Mockito.doThrow(new RuntimeException()).when(studentServices).saveorUpdate(Mockito.any(Student.class));

        Assertions.assertThrows(RuntimeException.class, () -> {
            studentController.update(student, "123");
        });
    }
}
