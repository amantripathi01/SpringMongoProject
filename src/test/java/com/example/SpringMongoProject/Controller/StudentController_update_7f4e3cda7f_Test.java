// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.beans.factory.annotation.Autowired;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
public class StudentController_update_7f4e3cda7f_Test {

    @InjectMocks
    StudentController studentController;

    @Mock
    StudentServices studentServices;

    @Test
    public void testUpdateStudentSuccess() {
        Student student = new Student();
        student.set_id("1234");
        student.setStudentname("Test Student");
        student.setStudentaddress("Test Address");
        student.setMobile("1234567890");

        when(studentServices.saveorUpdate(any(Student.class))).thenReturn(student);

        Student updatedStudent = studentController.update(student, "1234");

        assertEquals("1234", updatedStudent.get_id());
        assertEquals("Test Student", updatedStudent.getStudentname());
        assertEquals("Test Address", updatedStudent.getStudentaddress());
        assertEquals("1234567890", updatedStudent.getMobile());

        verify(studentServices, times(1)).saveorUpdate(any(Student.class));
    }

    @Test
    public void testUpdateStudentFailure() {
        Student student = new Student();
        student.set_id("1234");

        when(studentServices.saveorUpdate(any(Student.class))).thenReturn(null);

        Student updatedStudent = studentController.update(student, "1234");

        assertEquals(null, updatedStudent);

        verify(studentServices, times(1)).saveorUpdate(any(Student.class));
    }
}
