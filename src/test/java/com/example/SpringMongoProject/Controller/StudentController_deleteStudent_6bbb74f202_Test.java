// Test generated by RoostGPT for test gitlab using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import org.springframework.beans.factory.annotation.Autowired;

import static org.mockito.Mockito.*;

class StudentController_deleteStudent_6bbb74f202_Test {

    @Autowired
    StudentController studentController;

    @Mock
    StudentServices studentServices;

    @BeforeEach
    void setUp() {
        MockitoAnnotations.initMocks(this);
        studentController = new StudentController();
        studentController.studentServices = studentServices;
    }

    @Test
    void deleteStudent_validId() {
        String id = "1234";

        doNothing().when(studentServices).deleteStudent(id);

        studentController.deleteStudent(id);

        verify(studentServices, times(1)).deleteStudent(id);
    }

    @Test
    void deleteStudent_invalidId() {
        String id = "invalidId";

        doThrow(new RuntimeException()).when(studentServices).deleteStudent(id);

        try {
            studentController.deleteStudent(id);
        } catch (RuntimeException e) {
            // Expected exception
        }

        verify(studentServices, times(1)).deleteStudent(id);
    }
}
