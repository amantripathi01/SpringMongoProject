// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import static org.mockito.Mockito.*;
import static org.junit.jupiter.api.Assertions.*;

@ExtendWith(MockitoExtension.class)
public class StudentController_saveStudent_d0ac6d7812_Test {

    @InjectMocks
    StudentController studentController;

    @Mock
    StudentServices studentServices;

    @Test
    public void testSaveStudent_success() {
        Student student = new Student();
        student.set_id("12345");
        when(studentServices.saveorUpdate(any(Student.class))).thenReturn(student);
        
        String savedStudentId = studentController.saveStudent(student);
        assertEquals("12345", savedStudentId);
    }

    @Test
    public void testSaveStudent_failure() {
        Student student = new Student();
        student.set_id("12345");
        when(studentServices.saveorUpdate(any(Student.class))).thenReturn(null);
        
        String savedStudentId = studentController.saveStudent(student);
        assertNotEquals("12345", savedStudentId);
    }
}
