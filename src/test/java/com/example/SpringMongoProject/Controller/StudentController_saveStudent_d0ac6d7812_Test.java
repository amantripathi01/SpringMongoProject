// Test generated by RoostGPT for test gitlab using AI Type Open AI and AI Model gpt-4-1106-preview

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;
import static org.junit.jupiter.api.Assertions.assertEquals;

@ExtendWith(MockitoExtension.class)
public class StudentController_saveStudent_d0ac6d7812_Test {

    @Mock
    private StudentServices studentServices;

    @InjectMocks
    private StudentController studentController;

    private Student student;

    @BeforeEach
    public void setUp() {
        student = new Student();
        student.set_id("123");
        student.setName("Test Student");
        student.setEmail("test@example.com");
    }

    @Test
    public void testSaveStudent_Success() {
        // Arrange
        when(studentServices.saveorUpdate(student)).thenReturn(student);

        // Act
        String savedStudentId = studentController.saveStudent(student);

        // Assert
        assertEquals("123", savedStudentId);
        verify(studentServices).saveorUpdate(student);
    }

    @Test
    public void testSaveStudent_NullStudent() {
        // Arrange
        Student nullStudent = null;

        // Act
        String savedStudentId = studentController.saveStudent(nullStudent);

        // Assert
        assertEquals(null, savedStudentId);
        verify(studentServices).saveorUpdate(nullStudent);
    }
}
