// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

public class StudentController_saveStudent_d0ac6d7812_Test {

    @InjectMocks
    StudentController studentController;

    @Mock
    StudentServices studentServices;

    @Mock
    Student student;

    @BeforeEach
    public void init() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testSaveStudent() {
        // TODO: Change the value of studentId as per your requirement
        String studentId = "1";
        when(student.get_id()).thenReturn(studentId);

        String result = studentController.saveStudent(student);
        verify(studentServices).saveorUpdate(student);
        assertEquals(studentId, result);
    }

    @Test
    public void testSaveStudent_NullId() {
        when(student.get_id()).thenReturn(null);

        String result = studentController.saveStudent(student);
        verify(studentServices).saveorUpdate(student);
        assertEquals(null, result);
    }
}
