// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;

public class StudentController_getStudents_23978b7130_Test {

    @InjectMocks
    StudentController studentController;

    @Mock
    StudentServices studentServices;

    @BeforeEach
    public void init() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testGetStudents() {
        Student s1 = new Student();
        s1.set_id("1");
        s1.setStudentname("John");
        s1.setStudentaddress("123 Street");
        s1.setMobile("1234567890");

        Student s2 = new Student();
        s2.set_id("2");
        s2.setStudentname("Jane");
        s2.setStudentaddress("456 Street");
        s2.setMobile("0987654321");

        when(studentServices.listAll()).thenReturn(Arrays.asList(s1, s2));

        Iterable<Student> students = studentController.getStudents();
        assertEquals(2, ((Collection<?>) students).size());
    }

    @Test
    public void testGetStudents_Empty() {
        when(studentServices.listAll()).thenReturn(Arrays.asList());

        Iterable<Student> students = studentController.getStudents();
        assertEquals(0, ((Collection<?>) students).size());
    }
}
