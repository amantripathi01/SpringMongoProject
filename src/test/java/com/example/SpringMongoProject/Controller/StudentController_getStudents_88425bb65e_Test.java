// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.context.junit4.SpringRunner;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

@RunWith(SpringRunner.class)
@SpringBootTest
public class StudentController_getStudents_88425bb65e_Test {

    @Autowired
    private StudentController studentController;

    @MockBean
    private StudentServices studentServices;

    @Test
    public void testGetStudents_ValidId() {
        String studentId = "student1";
        Student student = new Student();
        student.set_id(studentId);

        when(studentServices.getStudentByID(studentId)).thenReturn(student);

        Student retrievedStudent = studentController.getStudents(studentId);
        assertEquals(studentId, retrievedStudent.get_id());
    }

    @Test
    public void testGetStudents_InvalidId() {
        String studentId = "nonexistent";
        when(studentServices.getStudentByID(studentId)).thenReturn(null);

        Student retrievedStudent = studentController.getStudents(studentId);
        assertNull(retrievedStudent);
    }
}
