// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package com.example.SpringMongoProject.Controller;

import com.example.SpringMongoProject.Entity.Student;
import com.example.SpringMongoProject.Service.StudentServices;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import org.springframework.beans.factory.annotation.Autowired;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.*;

public class StudentController_getStudents_88425bb65e_Test {

    @InjectMocks
    StudentController studentController;

    @Mock
    StudentServices studentServices;

    @BeforeEach
    public void init() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testGetStudents() {
        String studentId = "1234";
        Student mockStudent = new Student();
        mockStudent.set_id(studentId);
        mockStudent.setStudentname("John Doe");
        mockStudent.setStudentaddress("123 Street");
        mockStudent.setMobile("1234567890");

        when(studentServices.getStudentByID(studentId)).thenReturn(mockStudent);

        Student student = studentController.getStudents(studentId);

        verify(studentServices).getStudentByID(studentId);
        assertEquals(studentId, student.get_id());
    }

    @Test
    public void testGetStudents_notFound() {
        String studentId = "1234";
        when(studentServices.getStudentByID(studentId)).thenReturn(null);

        Student student = studentController.getStudents(studentId);

        verify(studentServices).getStudentByID(studentId);
        assertNull(student);
    }
}
